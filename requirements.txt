PySide6>=6.6
numpy>=1.23.0
sounddevice>=0.4.6
# R0 — execution core, real model calls, credential vault
litellm>=1.40
pydantic>=2.6
keyring>=24.0
# R1 — Agent tasks commonly need these inside the sandbox (data + charts)
pandas>=2.0
matplotlib>=3.7
# R2 — optional, only for richer file previews (panels degrade gracefully without)
pymupdf>=1.23
openpyxl>=3.1
python-docx>=1.1
python-pptx>=0.6
mammoth>=1.6
# R1+ — optional, local speech-to-text for long-press voice (degrades gracefully)
# Preferred backend: SenseVoice-Small via funasr (much better zh than Whisper)
funasr>=1.1
torch>=2.0          # funasr runtime (CPU build is fine)
torchaudio>=2.0
# Fallback backend
faster-whisper>=1.0
# T2 — optional, mount external MCP servers' tools (degrades gracefully without)
mcp>=1.0
# T3 — optional, headless browser tool for JS-rendered pages (run `playwright install chromium`)
playwright>=1.40
